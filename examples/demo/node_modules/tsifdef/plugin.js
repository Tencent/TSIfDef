// Demo shim so the workspace TypeScript can resolve the "tsifdef" tsserver
// plugin by name. It re-exports the built plugin from the repository dist
// output. A real install would resolve "tsifdef" to the published package.
module.exports = require("../../../../dist/tsserver/plugin.js");
