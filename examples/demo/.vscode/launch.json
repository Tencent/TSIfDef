{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Demo (Active Profile)",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/Build/.demorun/${command:tsifdef.activeProfile}/main.js",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "TSIfDef: Compile active demo",
      "sourceMaps": true,
      "outFiles": ["${workspaceFolder}/Build/.demorun/${command:tsifdef.activeProfile}/**/*.js"],
      "skipFiles": ["<node_internals>/**"]
    }
  ]
}
