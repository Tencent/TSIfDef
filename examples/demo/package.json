{
  "name": "tsifdef-demo",
  "private": true,
  "type": "module",
  "tsifdef": "./profiles/TEST_A.json",
  "scripts": {
    "build": "tsifdef build -- --noEmit false --outDir Build/.demorun/active --sourceMap true --inlineSources true --sourceRoot ../../../src",
    "watch": "tsifdef build --watch -- --noEmit false --outDir Build/.demorun/active --sourceMap true --inlineSources true --sourceRoot ../../../src",
    "legacy:precompile": "tsifdef",
    "legacy:compile": "npm run legacy:precompile && tsc -p .tsifdef/Output/tsconfig.json --noEmit false --outDir Build/.legacy/active --sourceMap true --inlineSources true --sourceRoot ../../../src"
  },
  "devDependencies": {
    "tsifdef": "file:../..",
    "typescript": "5.5.4"
  }
}
